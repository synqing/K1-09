// Minimal FixedPoints stub for host-side tests.
// Provides just enough to satisfy headers that rely on SQ15x16
// without pulling the full FixedPoints implementation.
#pragma once

#include <cstdint>

// Leave macros undefined exactly as the real header would unless needed

